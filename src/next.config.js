/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['d3d1owzsxs2d42.cloudfront.net', 'placehold.jp']
  },
}

module.exports = nextConfig
